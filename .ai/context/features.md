# Feature History & Backlog

## Current Version: pre-v0.1 (M0–M26 shipped in repo; M0–M4 verified live at the old vf-dev.lg4.ch, M5a–M13 verified live on `sdh` as of 2026-08-21 (`v0.4.8`) — see Host migration below; M14–M26 verified locally only, not yet deployed; M16–M19 deployed to `sdh` 2026-08-21 per the user, M20–M26 not yet)

> **M0–M4** were deployed via `scripts/VF-dev.ps1 deploy` (SSH alias `xpsex` → `/opt/VidFactory`)
> and verified live on that host, on git branch `m0-foundation` (local-only at the time).
> **As of M5b/2026-08-18** the repo is on GitHub at `Think4dvantage/VidFactory`, branch `main`
> (pushed), tagged `v0.2.0`–`v0.4.3` so far — each tag push auto-publishes the image to GHCR via
> `.github/workflows/docker-publish.yml`. M5a onward was verified locally only (pytest + manual
> `TestClient` runs) through M8.
> **2026-08-19: the host migration has actually happened** — found live during the M9 investigation
> below, not caught by the previous doc pass earlier the same day. `xpsex` is dead; the app now runs
> on a different shared docker host (SSH alias `sdh`, domain `vf.lenti.cloud`, not `vf.lg4.ch`),
> deployed from a compose project outside this repo. Base health (`/health`, mounts, sqlite) is
> confirmed **ok** there, running the current `main` HEAD. Full feature verification on that host
> (login, uploads, builds, a real Flightlog call) has **not** happened yet — see "Host migration"
> below and `architecture.md`'s Deployment section for the details found so far — incl. GPU/QSV not
> actually engaging (encoder reported `libx264` despite `/dev/dri` being passed through correctly),
> root-caused to a missing `libmfx-gen1.2` package and fixed in `Dockerfile` (M9), not yet deployed.
> The editor streams a 720p proxy (`preview_file`)
> for smooth scrubbing.
>
> **Pivot (post-M4):** flight-log/IGC analytics (M1, M1+…M1++++ below) has been **removed from
> VidFactory** and is becoming a separate "Flightlog" project/service with its own API — which now
> exists and is integrated (see M6). VidFactory is purely the video-production tool; `Project` is
> standalone (no more `Outing` dependency). The M1* rows below are kept as history of what shipped
> and when — that code and its live data (598 outings, 281 igc_tracks rows) are **not deleted from
> the DB**, just no longer referenced by this app. See M1a below for the detach. The home NAS is
> also unavailable for the next year (house move + confirmed timeline, M7's trigger), hence M1a's
> upload path and M7's removal of NAS video browsing entirely.

### Shipped Milestones

| Milestone | What shipped |
|---|---|
| M0a | `.ai/` foundation: dev-web blueprint adopted + filled (VidFactory model, FFmpeg pipeline, mounts, deploy contract). |
| M0b | App skeleton + deploy: FastAPI, `config.py`, SQLite/SQLAlchemy + `db.py` (.sql migrations), ported `ffmpeg_runner`/`gpu_detector`, jobs+SSE, server-side file browser, Dockerfile/compose/`VF-dev.ps1`, GHCR publish workflow. **Deployed live at `vf-dev.lg4.ch`** (HTTP 200, LE cert). `/health` is liveness-based (200 "degraded" when only NAS mounts missing). |
| M1 | Flight log: importer (598 flights + sites from `Flugbuch.xlsx`, frequencies match the audit), outings+sites CRUD, rollups (hours/site-frequency/seasonality), JSON API, Jinja+HTMX UI. **Imported & verified live.** |

| M2 | Concatenate → Full Flight: project-from-outing, ordered source parts (reorder/remove), optional hike prepend (speed_factor; pre-sped = copy), stream-copy concat with SSE progress, NVENC for the sped hike. **Verified live**: 20260502 two 4K parts → `fullflights/20260502_FullFlight.mp4`, duration == sum of parts. |

| M3 | Highlights + Summary: highlight editor (range-streamed full flight, IN/OUT marks, names/roles/flags, timeline), `merge_overlaps` + `auto_fill`, single-pass filter_complex summary (drawtext overlays via textfile, picture highlights, NVENC), music service (folder/file, loudnorm bed, credits), full-flight-with-music (stream-copy + amix). **Verified live**: 20260502 → `Summary.mp4` (60s, overlays+music+credits) and `FullFlight_withMusic.mp4` (6.9GB). |

| M4 | Shorts: highlight-driven (hook→launch?→flying→landing?→CTA, capped <30 s, titled from highlight) + random-pool batch with `UsedMap` de-dup; vertical 1080×1920 normalize (centre-crop), CTA end screen (drawtext), per-short music; NVENC; history in DB. **Verified live**: highlight short 26.0s (hook+launch+3 flying+landing+CTA) and 2 random shorts with no shared flying footage. |
| M1+ | Flight-log polish: search/year/category/glider filters + sortable columns + pagination (`partials/outings_table.html`); 2-line rows (stats + buddies/comment). Managed dropdowns via a `lookups` table (migration 0003, seeded) in a "Manage Dropdown Data" modal; `launch_type` normalised `f/F→forward`, `r→reverse`. xlsx-import button replaced by **CSV export** (`importer` stays a CLI path). **Verified live.** |
| M1++ | Flight-log analytics & logbook fields: dedicated **`/flightlog/stats`** page (records + year-over-year matrices: category×year, launch-type with reverse-% per year + overall, buddy×year, Hike&Fly totals/averages). **Flight buddies** (`buddies`+`outing_buddies`, migration 0004, seeded Tom/Ueli/Päsci/Simon/Johannes) as a per-outing multiselect, managed in the same modal. **Hike&Fly metrics** (`climb_m`/`hike_distance_km`/`hike_duration_min`) shown on the form only when category=Hike&Fly (toggled by `static/app.js`). **Verified live**: reverse share 34.6% (207/598); Hike&Fly 97 across years. |
| M1+++ | **IGC track analysis** (Phase 1): per-outing IGC upload (edit → IGC track) saved to `VF_IGC` (`/data/igc`, on the `pg` share), analyzed by `core/igc.py` via **libigc** → `igc_tracks` aggregates: **cumulative climb across all thermals** (the headline ask — ≠ max−launch), thermal count, best/avg climb rate, max alt, glide count/distance, achieved glide ratio. Surfaced on the form, a table badge (🌀 N thermals · ↑m), and a "Climbs by year" rollup on the stats page. New runtime dep → Docker rebuild. A *thermal* = circling with **net climb** (paraglider tuning: libigc otherwise counts descending spirals). **Verified live** on 113 real 2019–21 tracks. |
| M1++++ | **IGC bulk import** (Phase 2, `core/igc_import.py`): "📈 Import IGC" → dry-run modal classifies every unlinked file in `pg/igc` as a clean match (exactly 1 untracked outing + 1 file that date) or manual (multi-flight/multi-file/no-outing/already-tracked); one click imports the clean set. Idempotent. **Live state (full XContest library, 627 files): 281 flights auto-tracked, ~76 km cumulative climb, 574 thermals; ~124 multi-flight dates left for manual.** Device↔XContest duplicates were de-duped: 51 redundant device (`*-XTR-*.IGC`) files staged in `pg/igc/.device_dupes/`, 12 tracks re-pointed+re-analyzed to the XContest twin, 11 device-only 2019 sled rides kept. **(Phase 3, multi-flight-day matching, was never started — flight-log was extracted to its own project before that work began; superseded by M1a.)** |
| M1a | **Detach from flight-log + local upload:** `Project` no longer depends on `Outing` — `outing_id`/relationship removed from the ORM (physical column + `outings` table left untouched in the live DB), replaced by nullable `date` + `external_flight_id` (opaque future reference, migration `0006`). All flight-log code/routes/templates deleted (`/flightlog*` now 404). New `core/flightlog_client.py` seam (`get_flight_metadata()`, `NotImplementedError`) is the documented plug point for the future Flightlog API. Dashboard gained a Projects list + "New Project" form as the new entry point (`POST /projects/new`). Added local-disk raw-video upload (`POST /api/projects/{id}/parts/upload`, chunked streaming to a new `vf_uploads` volume, `VF_UPLOADS_DIR`) since the NAS is unreachable post house-move; uploaded parts are kept indefinitely (new host has 55 TB). **Verified locally via TestClient** (project create → upload → SourcePart on disk); live-host verification pending the new deploy target. |
| M1b | **Standalone deploy example:** this repo doesn't deploy itself — it now ships a `docker-compose.standalone.yml` **example** (pulls the already-published `ghcr.io/think4dvantage/vidfactory` image; no Traefik/`proxy` network, no `/mnt/pg` NAS bind mount, publishes `8000:8000` directly) plus `.env.example`, for another project to copy and run on its own host. Music (`VF_MUSIC_HOST`, read-only) and outputs+archive (`VF_LIBRARY_HOST`) bind-mount host folders instead of living under the NAS mount; `VF_VIDEOS` stays unmounted (degrades gracefully, upload is the working path per M1a). GPU devices kept in the example but NVIDIA CDI commented out (target host has Intel QSV via dedicated card, no NVIDIA toolkit yet). Zero app-code changes — `config.py`/`music.py` already took every root via env override. |
| M5a | **YouTube metadata API** (`core/youtube_meta.py`, `api/routers/youtube.py`): `GET /api/projects/{id}/youtube-metadata` — pivoted from the original file-handoff plan to a **live read API only** (no `metadata.json`), for a separately-run YouTube-management container to pull on demand over the published port and derive its own chapters/titles/descriptions. Returns every `Highlight` (full-flight timestamps), `segment_order` (content order from `merge_overlaps()`, explicitly *not* a rendered-video timestamp — see the module docstrings), and every `Short` with `segments_used` + resolved `source_highlight_name`. Added `config.flightlog.base_url`/`VF_FLIGHTLOG_URL` as a placeholder (unused — `flightlog_client.py` stays a stub, not called from this endpoint; `external_flight_id` is returned raw). First tests in the repo: `tests/backend/test_youtube.py` (in-memory SQLite + `TestClient`, dependency-overridden `get_db`), 2 passing. |
| M5b | **Multi-user accounts:** `users`/`sessions` tables (migration `0007`), opaque session-cookie login (`scrypt` password hashing, no `passlib`), `Project.owner_id` + ownership scoping across every router (a project/job that exists but isn't yours 404s, same shape Flightlog itself uses), `core/jobs.Job.owner_id` scoping the job registry/SSE, `GET/POST /login`, `POST /logout`, `GET /account`. No signup UI — first user comes from `VF_BOOTSTRAP_USERNAME`/`_PASSWORD` at first boot (`core/auth.ensure_bootstrap_user`, backfills any pre-existing ownerless projects to them), a second user via the new `scripts/create_user.py`. Output filename collision fix: `core/projects._stem()` now includes the project id (was date-only, so two users flying the same day would have overwritten each other's outputs in the shared output roots). **Verified locally via TestClient/pytest** (`tests/backend/test_auth.py`); no live host to deploy to yet. |
| M6 | **Flightlog API integration:** Flightlog now exposes a live, frozen `/api/integration/v1` contract; `core/flightlog_client.py` is a real `httpx`-based client (moved `httpx` to runtime deps) instead of the M1a stub. `models/flightlog.py` mirrors the contract verbatim (`FlightMetadataOut`, `SegmentOut`, `FlightLink`, `IgcSummary`). Migration `0008` retypes `external_flight_id` to `TEXT` (the contract's flight id is a string; the old `Integer` column would have silently mishandled it — no data was ever at risk, the field was always `NULL` until now). Each user supplies their own Flightlog API key on `/account` (per-pilot bearer credential, plaintext at rest since it must be resent verbatim, never returned in any response). `youtube-metadata` gained best-effort `flight`/`flight_segments` keys (silently `null` if not configured or the call errors — never breaks the endpoint). New `POST /api/projects/{id}/flightlog-id` (set the flight id on a project) and `POST /api/projects/{id}/flightlog-link` (push a finished-video URL back to Flightlog — called by the external YTChannelMgmt MCP once it actually publishes, since VidFactory's own outputs are local files with no public URL to push automatically). **Verified locally via `httpx.MockTransport`** against the documented contract shapes (`tests/backend/test_flightlog_client.py`); a live call against the real `fl-dev.sdh.lol:8002` dev key could not be verified from this dev environment (outbound network egress is blocked here) — verify from a host that can reach it before relying on it in production. |
| M7 | **Removed the `videos` NAS mount entirely** (`config.MountsSection`/`mount_roots()`/`_ENV_MOUNT_KEYS` no longer have a `videos`/`VF_VIDEOS` entry) — the NAS it pointed at (`InstaOut`) is confirmed unreachable for the next year, so the "browse NAS, add selected parts" UI/endpoint (`POST /api/projects/{id}/parts/add`) and the NAS-based Hike & Fly source picker are both gone; local upload (`POST /api/projects/{id}/parts/upload`, from M1a) is now the sole way to add any source footage. Hike & Fly footage gets its own upload path: `POST /api/projects/{id}/hike/upload` (multipart, appends to `Hike.sources` + sets `speed_factor`) and `POST /api/projects/{id}/hike/remove` (drops one entry by index) replace the old NAS-checkbox `set_hike` endpoint. The `browser`/`filebrowser` mechanism itself is untouched — it still serves `music`, which was already local-only in the standalone deploy (M1b) and never depended on the NAS. **Verified locally** (pytest + a manual TestClient run: project page has no NAS references, `/api/browse/videos` 400s, hike upload/remove round-trips, `/browse/music` still works). |
| M8 | **Flightlog timeline hints in the highlight editor:** `core/flightlog_hints.py` translates Flightlog IGC segments (`GET .../segments`) into video-timeline positions using the pilot's own `launch` highlight as the anchor (`video_offset_s = launch.start + segment.start_offset_s` — Flightlog has no idea when the camera started rolling, only "seconds since takeoff"). New `GET /api/projects/{id}/flightlog-hints` — always 200, `{"status": "no_launch_marked"\|"unavailable"\|"ok", "hints": [...]}`, best-effort exactly like the M6 `youtube-metadata` enrichment (Flightlog outage/no key/no launch marked never breaks the editor page). `static/editor.js` overlays small colored markers on the timeline canvas keyed by `hint.kind` with a generic fallback style for any kind not in the lookup table — deliberately future-proof, since Flightlog may add more segment kinds later and none of this code should need to change when it does. Hover a marker for a tooltip (kind, altitude change, climb rate); clicking within a few pixels of one snaps the seek to it exactly. Refetched every time the highlight list reloads, so hints appear live the moment a launch highlight is saved. **Verified locally** (pytest incl. offset-math assertions in `tests/backend/test_flightlog_hints.py`, plus a manual TestClient run through create-project → editor page → mark launch → hints endpoint). |
| M9 | **Fixed source-video upload 500s + dependency-floor audit:** every part/hike upload was failing server-side with `multipart.exceptions.MultipartParseError: Did not find CR at end of boundary` — a `python-multipart` 0.0.9 boundary-parser bug that only surfaces on large multipart bodies, not the tiny `TestClient` uploads M1a's local verification used, so it shipped unnoticed. Root cause: `pyproject.toml` pinned it `^0.0.9`, and Poetry's caret on a `0.0.x` version locks to that exact patch — with no `poetry.lock` committed (deliberate, so every image build re-resolves to latest), every rebuild kept re-landing on the same broken `0.0.9`. Fixed the constraint (`>=0.0.32,<1.0`, the actual latest available today) and audited every other dependency for the same caret-on-`0.x` trap (`fastapi`, `uvicorn`, `httpx` were minor-locked the same way; `pillow`/`sse-starlette`/`pytest` were major-capped below latest) — see `01-project-overview.md` "Dependency versioning". Also removed `aiofiles` (declared, never imported anywhere) and bumped the `htmx` CDN pin (`2.0.3`→`2.0.10`). Separately, the upload `<form>`s had zero progress/error UI (`hx-swap="none"`, no listener) — indistinguishable from "broken" even once uploads succeed, since a multi-GB file over a home connection can take a long time with no feedback at all; replaced both forms with `XMLHttpRequest`-based uploads showing live byte progress and surfacing any HTTP error inline (`templates/project.html`). **Verified live**: reproduced the exact `MultipartParseError` in `sdh`'s live container logs before the fix, then — after the user deployed `v0.4.1` — confirmed via the same container logs that it's gone; uploads now fail differently (a Traefik 60s proxy timeout, unrelated, see the Host migration roadmap row and M10) rather than 500ing before a byte is written. |
| M10 | **Fixed QSV hardware encode never actually engaging on `sdh`** (`Dockerfile`): `/health` reported `libx264` despite `/dev/dri` being correctly passed through — `gpu_detector.py`'s `h264_qsv` test-encode was silently failing every time. Root-caused live on `sdh`: the host's single GPU is a discrete Intel Arc A380 (DG2 — `lspci` shows no separate integrated GPU on this box); VAAPI/the `iHD` driver load fine, but ffmpeg's actual QSV encoder needs a further layer on top — the oneVPL GPU runtime, Debian package `libmfx-gen1.2` — which the image never installed (only `va-driver-all`, the VAAPI layer itself). Jellyfin on the same host was unaffected because it bundles its own self-contained `jellyfin-ffmpeg` build. Confirmed as the real fix by installing `libmfx-gen1.2` live in the running container (ephemeral, not persisted — gone on next recreate) and re-running the exact failing test-encode: clean exit. Added `libmfx-gen1.2` to `Dockerfile`'s apt install list for good. **Deployed and confirmed live 2026-08-20** — `sdh` runs `v0.4.3`, `/health`'s `encoder` field reads `h264_qsv`, and a real Summary+shorts build's container logs show every re-encode using `-c:v h264_qsv` (the concat and full-flight-with-music steps are `-c copy` by design, so they never touch the encoder). |
| M11 | **Chunked, resumable source upload** (`core/chunked_upload.py`): the single-request whole-file multipart upload (M1a/M9) hit a hard wall on `sdh` — Traefik's `websecure`/`web` entrypoints default `respondingTimeouts.readTimeout` to **30 minutes** (confirmed via Traefik's own `/api/entrypoints`, and via a live 8 GB upload that died at exactly `1800.000087s`, `502` + `readfrom ... i/o timeout`), and the user wants that global default left alone rather than raised (it's a sane cap for the other services behind the same proxy). So VidFactory now works around it instead of depending on infra: the client splits the file into 8 MiB chunks (`POST .../upload/begin` → `PUT .../upload/chunk?filename=&offset=`, repeated → `POST .../upload/finish`), each a fast, independent request nowhere near 30 minutes. Resumability is a side effect of the design, not extra work: there's no in-memory session state at all — `begin` just reports the current size of the staging file on disk, so a laptop sleep, network drop, or even a container restart mid-upload only costs the bytes since the last chunk; re-clicking Upload with the same file picks up from the real on-disk offset. A `409 {"offset": actual}` on `chunk`/`finish` handles a chunk that partially landed before a disconnect — client resyncs rather than getting stuck. Old whole-file `POST .../parts/upload`/`.../hike/upload` endpoints and the M9 XHR-progress handler were deleted outright, not kept alongside (`04-constraints.md`: no compat shims). **Verified**: `tests/backend/test_chunked_upload.py` covers begin→chunk→chunk→finish, resume returning a nonzero offset, and the offset-mismatch 409 path — but per `06-testing-conventions.md`'s in-memory-SQLite/`TestClient` limits (no real multi-GB transfer, no real Traefik in front), the only real proof is one full 8-10 GB upload against `sdh` after this deploys, watched via the staging file's size growing — exactly the gap that let the M9 multipart bug ship unnoticed in the first place. Not yet deployed. |
| M12 | **API-key integration contract** (`api/routers/integration.py`): a public, API-key-authenticated mirror of the `youtube` router's read/link-push endpoints, for the external YouTube-management repo to pull project data over the internet without a browser session. Deliberately **per-user, not a single global key** — each user generates/regenerates their own on `/account` (`POST /api/account/api-key`, `hx-post`+`HX-Redirect` like the Flightlog-key form so a page refresh can't silently mint a new one; stays visible on later visits since only its own owner can load that page, `users.api_key` migration `0009`, plaintext for the same reason as `flightlog_api_key`), so a leaked or shared key still can't see another user's projects; keeps the M5b ownership boundary intact. `require_api_user` (`api/auth_deps.py`) resolves `Authorization: Bearer <key>` to a `User`; every route under `/api/integration/v1` (`GET /projects` discovery listing, `GET /projects/{id}/youtube-metadata`, `POST /projects/{id}/flightlog-link`) calls the exact same handlers the session-authed routes use — zero duplicated business logic, only the auth dependency differs. **Verified locally via TestClient** (`tests/backend/test_integration_api.py`: 401 with no/bad key, 200 + correct data with a valid key, cross-user 404 isolation, the account-generated key authenticating end-to-end, a page refresh not rotating the key). **Deployed and live on `sdh` as of 2026-08-21** (`v0.4.8`) — still not yet exercised by the real external YouTube-management repo. |
| M13 | **Global FFmpeg job queue + elapsed-time display** (`core/jobs.py`): every build (concat, preview, summary, fullmusic, shorts) now goes through a single FIFO queue with one worker thread, so exactly one job runs at a time across the whole app — the host has one GPU encoder and is already CPU-contended by other unrelated services on `sdh`, so concurrent builds just make each other slower. `registry.run()` used to spawn a thread per job that started immediately; now it enqueues and the job sits `pending` behind whatever's ahead of it. `registry.position_in_queue()` surfaces how many jobs are ahead (merged into every SSE/`/api/jobs` payload as `queue_position`), and `Job.started_at`/`finished_at` back a new `elapsed_seconds` field — both driven by the same trigger: a queued job showing bare "pending" at 0% for up to an hour is exactly the ambiguous state that led to the `v0.4.6` double-build corruption, so the UI now shows "queued — N ahead" and a live/final elapsed time instead of leaving it looking stuck. Cancelling a still-queued job now skips it without ever running its target; cancelling a running one is unchanged (`ffmpeg_runner.encode()`'s existing `cancel_event` check + `proc.terminate()`) — noted as the only escape hatch if a job ever hangs without emitting ffmpeg progress, since every other queued job now waits behind it. `/health` gained `jobs_queued` alongside `jobs_active`. The `v0.4.6` same-kind-same-project conflict guard (`find_active()`) is kept on top of the queue — it stops identical builds from being queued back-to-back and redoing the same work, which the FIFO queue alone wouldn't prevent. **Verified locally via `tests/backend/test_jobs.py`** (jobs run strictly one at a time not concurrently, FIFO queue-position ordering, cancelling a queued job never invokes its target, elapsed time is `None` while queued and tracks real duration once run). **Deployed and live on `sdh` as of 2026-08-21** (`v0.4.8`, confirmed via `/health`'s new `jobs_queued` field) — not yet observed processing a real multi-job queue (only ever seen with 0 queued/0 active so far). |
| M14 | **Browse: delete + multiselect download.** `core/filebrowser.delete()` + `POST /api/browse/{root}/delete` remove one or more files, restricted to `cfg.writable_roots()` (the output/archive roots — `music` stays read-only, matching how it's always been the one root the app never writes to); a bulk request reports per-item failures inline rather than failing the whole call, and there's still no recursive directory delete. `templates/partials/listing.html` gained a selection toolbar (select-all, download-selected, delete-selected) and a per-row 🗑; `static/browser.js` (new, event-delegated on `#listing` so it survives htmx swaps) wires the checkboxes, fires "download selected" as N staggered `<a download>` clicks against the existing single-file endpoint (no new zip/bulk-download endpoint), and does the per-row delete as a plain `fetch()` rather than htmx `hx-vals` — a filename with a literal `"` would otherwise break the JSON embedded in that HTML attribute (the bulk "delete selected" button doesn't have this problem since it's a real form submit, not JSON-in-an-attribute). **Verified locally via `tests/backend/test_browser_delete.py`** (delete on a writable root, rejected on `music`, path traversal, directory-delete rejected, bulk endpoint round-trip) — not yet deployed/exercised on `sdh`. |
| M15 | **Shorts: hike segment + distributed flying picks.** User review of the 12.07.2026 project found every random flying clip clustered in the last few minutes of the flight — none from the prepended hike, none from mid-flight scenery (e.g. over Mürren). Root cause: `shorts.pick_clips()` chose among leftover *windows* with equal probability regardless of window size, so once a small region got sampled, subtraction fragmented it into many competing tiny windows that out-voted any single large untouched region — clustering by chance, not design. Fixed by bucketing the pool into `count` equal-width time buckets and drawing one clip per bucket (`core/shorts.py`); `_flying_pool()` now excludes the prepended hike segment (`core/concat.hike_output_duration()`, new — recomputes the hike's on-timeline length since that boundary was never persisted). New required composition for every short, both modes: `hook? → hike×2? → launch? → 3 flying (spread across the flying time) → landing? → CTA` — hike pair only for `flight_type == "hike_and_fly"` (`ShortsSection.hike_clip_count`, default 2), otherwise identical order minus that pair. Drops the M4 "<30s" framing for Hike & Fly shorts (32s with defaults) — never enforced by code, just no longer true as a design note. A user-marked `launch`/`landing` inside the hike segment isn't clamped (not our call to override a manual mark) but now logs a warning so a repeat of this class of bug is diagnosable. **Verified locally** (`tests/backend/test_shorts_engine.py`: bucketed spread, no-overlap, empty-pool; `tests/backend/test_shorts_build.py`: full hike-and-fly composition order end-to-end via a mocked `_shorts_target`, normal-flight has no hike clips, the mismarked-launch warning fires) — not yet re-verified against a real rebuild of the 12.07.2026 project on `sdh` (not yet deployed). |
| M16 | **Prune shorts with deleted files from the project page.** After using the M14 file-browser delete on some unbalanced 12.07.2026 shorts, their `Short` DB rows kept showing up on the project page with nothing left to play/download. `core/projects.prune_missing_shorts()` now runs before `GET /projects/{id}` renders: any `Short` whose `output_file` no longer exists on disk is deleted from the DB (not just hidden from the list) and logged at INFO, mirroring the on-demand `Path.exists()` pattern `fullflight_video`/`editor_page` already use elsewhere — a `Short` is a disposable generation record, so an actual delete (vs. filtering only, which would re-show the same phantom row forever) is the right cleanup. **Verified locally** (`tests/backend/test_shorts_pruning.py`: missing files pruned and removed from the DB, existing files kept untouched, end-to-end through the real page route) — not yet deployed. |
| M17 | **Job Queue page** (`GET /jobs`, `api/routers/sse.py`): the M13 global FIFO queue had no view of its own — the dashboard only ever showed a bare "Active jobs: N" count, and a job's progress was only visible on the one project page that started it. `/jobs` lists every job for the current user (any project, any status: queued/running/done/error/cancelled) with project link, stage, live progress bar, elapsed time, and a cancel button for anything still queued or running — all data the M13 `/api/jobs` endpoint already returned (incl. `queue_position`), so no new backend surface beyond the page route itself. `static/jobs.js` polls `/api/jobs` every 2s and re-renders the table client-side (plain JSON, not HTMX — same pattern `project.html`'s own build-progress code already uses, not a new one); newest-first via `registry.list()`'s insertion order reversed, no new fields needed on `Job.public()`. Nav gets a "Queue" link; the dashboard's job count now links to it too. **Verified locally** (`tests/backend/test_jobs_page.py`: login-gated, renders, nav link present) — not yet deployed. |
| M18 | **Music copyright/attribution text, generated not just listed.** The music library ships under `resources/StreamBeats Sync_Use License.pdf` (Senpai Music Group, LLC's Synchronization and Master Use License) — clause 7 asks "reasonable efforts" to credit the author's name and each track's title wherever the music is used. `write_credits()` used to just dump a bare track-name list; `core/music.py` now has `resolve_track_credits()` (probes each track's `title`/`artist` tags once, at build time), `build_credits_text()` (pure formatter — no filesystem paths, no full legal text, just the per-track credit lines the license actually asks for plus one line naming the license covering the library — never asserted per-track, since we can only attribute what a file's own tags say) and `write_credits()` now takes those resolved credits directly (signature changed; both existing call sites — Summary, FullFlight+music — updated, no behavior change for them beyond the nicer text). **Shorts now get this too** (the actual gap that prompted this — no per-short credits existed at all before): `_shorts_target` resolves credits once per built short, writes the sibling `_MusicCredits.txt` (mirroring Summary/FullMusic), and — since a `Short.segments_used["music"]` already existed as a single track path — stores the *full* resolved credit list there instead, so the project page can render it without touching ffprobe or the filesystem again. `GET /projects/{id}` now shows a copy-pasteable "🎵 Music credits" textarea (readonly + a `data-copy-target` Copy button, `static/app.js`) under Summary, Full flight + music (read from the sibling file — no DB record for those existed before or after this, so file-read is the only option) and **every already-existing Short**, via `music.normalize_music_credits()` handling the old single-string shape so shorts built before this change show credits too, not just ones built after. **Verified locally** (`tests/backend/test_music.py`: resolve/normalize/build/write; `tests/backend/test_music_credits_page.py`: current-shape, legacy-string-shape, no-music, and file-based summary credits all render correctly on the real page route) — not yet deployed. |
| M19 | **Music credits exposed via the YouTube-metadata API** (both `GET /api/projects/{id}/youtube-metadata` and its API-key-authed mirror under `/api/integration/v1` — same handler, so this was a one-place change). Prompted directly by asking whether the external YouTube-management tool (M12) could actually pull the M18 pasteable text itself rather than a human copying it off the project page. `ShortMeta.credits` (per short, resolved from `segments_used["music"]`, `None` if no music) and `ProjectYoutubeMetadata.summary_credits`/`fullflight_music_credits` (top-level, read from the sibling `_MusicCredits.txt` files — no DB-backed track list exists for those two builds, same file-only constraint M18 already had for them) — both fields had to be added to the Pydantic response models too, not just the dict `core/youtube_meta.build_metadata()` returns, since the routes declare `response_model=ProjectYoutubeMetadata` and FastAPI silently strips any key not in the schema. **Verified locally** (`tests/backend/test_youtube.py`: per-short credits incl. the legacy string shape and no-music case, summary credits from the sibling file; `tests/backend/test_integration_api.py`: the same data over the API-key path, the one an external tool without a browser session would actually use) — not yet deployed, and still not yet exercised by the real external tool (same caveat M12 already carried). |
| M20 | **Fixed StreamBeats filename attribution** (`core/music.py`) — deployed M18/M19, then a real Summary credits copy-paste came back still showing the old `Music Credits\n=====` header with `21. Harris Heller - High Tide` as one lump title. Two separate things, both fixed: (1) that specific Summary was simply built before the deploy — Summary/FullMusic credits are read from a sibling file written at *build* time (no DB record, see M18), so a pre-existing one only updates on the next rebuild, not retroactively; not a bug, just a real gap now called out explicitly wherever that caveat matters. (2) the actual bug: StreamBeats' files ship with no embedded ID3/format tags at all, only a filename convention (`"<N>. <Artist> - <Title>.<ext>"`), so `resolve_track_credits()`/`normalize_music_credits()` were dumping the *whole* filename as the title with no artist. Added `_split_filename_credit()` (regex on that exact convention) as the fallback whenever both tag lookups come back empty, so `"21. Harris Heller - High Tide.mp3"` now resolves to title `"High Tide"` / artist `"Harris Heller"` instead of one unsplit string. **Verified locally** (`tests/backend/test_music.py`: the exact filename from the live report, plus the legacy-single-string-shape path through the same parser) — not yet redeployed. |
| M21 | **Manual guest-pilot metadata.** Prompted by wanting to publish footage a non-Flightlog-using relative hands over. New nullable `Project.pilot_name` (migration `0010`, `core/projects.create_project`/`set_pilot_name`, `POST /projects/new` + `POST /api/projects/{id}/pilot-name` — mirrors the existing `flightlog-id` field exactly) — deliberately just a free-text name, no manual site/glider/duration, consistent with how the rest of the app already leaves that data to Flightlog. A non-null value doubles as the flag (no separate boolean): a `👤 {name}` badge on the dashboard project list and the project page header marks it as not the owning user's own flight. Also exposed as `pilot_name` on `/api/projects/{id}/youtube-metadata` (both session and API-key paths — same handler) so the external YouTube-management tool can credit the actual pilot when these get published. **Verified locally** (`tests/backend/test_projects.py`: create with/without pilot_name, set/clear round-trip, cross-user 404; `tests/backend/test_youtube.py`: pilot_name present/absent in the metadata response) — not yet deployed. |
| M22 | **Skip the concat step for a single-part flight.** `core/concat.concatenate()` now copies the one source file straight to the full-flight output path (`shutil.copy2`) instead of running it through the ffmpeg concat demuxer, whenever there's exactly one source part and no Hike & Fly footage to prepend — that case needs no concat at all, and the remux step was pure overhead (still a full read+write of a 60–300 GB file for zero benefit). Stage/progress callbacks still fire (`"Copying"`, then 100%) so the UI behaves the same either way. **Verified locally** (`tests/backend/test_shorts_engine.py`: single-part build copies the file byte-for-byte and never touches the GPU encoder; stage/progress callbacks fire correctly) — not yet deployed. |
| M23 | **Picture highlights (upload) + combined-highlight-length estimate.** The `Highlight.type`/`image_path`/`duration` columns and the summary/`youtube_meta` rendering logic already supported a `"picture"` highlight (a still image composited into the Summary for a fixed duration) since M3, but nothing let you actually create one — `editor.js` only ever sent `type: "video"`. Added `POST /api/projects/{id}/highlights/picture-upload` (plain multipart, not the chunked machinery M11 built for multi-GB source video — images are a few MB) validating the extension against `filebrowser.IMAGE_EXT`, storing under `uploads_dir/{project_id}/pictures/`; and `GET /api/projects/{id}/highlights/{highlight_id}/picture` to serve one back (ownership-checked) for the editor's preview thumbnail. The Mark-highlight form gained a Video/Picture toggle: picture mode swaps OUT for an image picker (auto-uploads on file select, local `URL.createObjectURL` preview before the upload even completes) + a duration field, and disables Role/"make short" — both feed straight into ffmpeg trims of the full-flight video elsewhere (shorts hook/launch/landing), which a picture (no meaningful video range) would break. Also added a combined-length readout next to the highlight list (`Highlights (N) — combined length: M:SS`), computed client-side by mirroring `core/highlights.py:merge_overlaps()` (overlapping video ranges collapse into one span, pictures add their own duration, only `use_in_summary` highlights count) — this is exactly the number `build_summary_ep` would produce as the floor length before `auto_fill` padding kicks in, so it tells the user how long a Summary would be with *only* the marked highlights, to help pick a sensible target length. **Verified locally** (`tests/backend/test_highlight_picture.py`: upload→attach→list→serve round-trip, unsupported extension rejected, cross-user 404 on upload and on serving, 404 when a highlight has no image; a `TestClient` render check confirms the new editor.html elements are present) — not yet deployed. |
| M24 | **Shorts: one queued job per short, not one job for the whole batch.** A highlight-driven "Build Shorts" batch (or a random-pool one with `count` > 1) used to run as a single `"shorts"` job that looped internally, so the Job Queue page (M17) always showed exactly one row no matter how many videos were actually being produced — no visible sign of count or per-short progress. `build_shorts_ep` now resolves the per-short task list (hook range + title + source highlight id for highlight mode, `count` blank tasks for random mode — cheap: DB only, no ffprobe) up front and calls `registry.run()` once per task, each carrying the highlight's name as a new `Job.title` (queue page shows e.g. "shorts — Takeoff"); `_shorts_target`'s internal loop was split into `_short_target`, building exactly one short per call. The one piece of state that has to stay *shared* across a batch — the `UsedMap` that keeps two shorts in the same batch from reusing the same flying/hike footage — is created once in the endpoint and closed over by every job's target; safe because the global FIFO queue (M13) still runs jobs strictly one at a time, so those closures never actually execute concurrently. `core/jobs.Job` gained the `title` field (threaded through `registry.run()`, `Job.public()`, so it reaches `/api/jobs` and the SSE stream for free); `static/jobs.js` shows it next to the kind. `templates/project.html`'s per-project progress bar now walks the returned `job_ids` one at a time (`followShortsBuild`, only one ever runs at once anyway) instead of following a single job, and `resumeActiveJobs()` collects every non-terminal `shorts` job for the project rather than re-binding per job. Tradeoff, not asked about but worth flagging: cancelling is now per-short (the Job Queue page's cancel button on one row no longer stops the rest of the batch the way cancelling the old single job did) — nobody has asked for a bulk-cancel-the-batch control, so one wasn't built. **Verified locally** (`tests/backend/test_shorts_build.py`: highlight mode queues exactly one job per `make_short` highlight with the right titles, random mode queues `count` distinct jobs, zero flagged highlights 400s immediately instead of failing a job after the fact; the existing composition-order/hike-exclusion/mismarked-launch-warning tests updated to call `_short_target` directly) — not yet deployed. |
| M25 | **CTA end screen on every produced video, not just shorts.** Only `shorts.build_short` ended on the "for more relaxed Paragliding / Like & Subscribe" CTA (`ShortsSection.cta_*`, `Config.cta_image`) — Summary and Full flight + music both ended abruptly on the last highlight/frame. `core/summary.py:build_summary()` now appends the CTA as one more `filter_complex` concat input (same technique already used for picture highlights: looped still image + `anullsrc` + two centred `drawtext` lines, sized relative to the project's own height rather than shorts' fixed 1920px-tall frame) — it rides through the same single-pass re-encode Summary already does, and since the music mix runs on the *combined* `[outa]` (concat happens first in the filtergraph), music keeps playing under the CTA exactly like it already does for shorts. `build_fullflight_with_music()` is architecturally different — it deliberately stream-copies the (often multi-hour, 4K) video and only re-encodes audio, so re-encoding the whole thing through a filter_complex just to add 3 seconds would have been a real cost regression; instead it now encodes the CTA separately at the flight's own resolution/fps and joins it with the concat demuxer (`-c copy`), the same "normalize a short segment to spec, then stream-copy concat" trick `core/concat.py` already uses to prepend a sped-up hike segment. Both `_summary_target`/`_fullmusic_target` (`api/routers/projects.py`) now compute/pass a `gpu` (fullmusic didn't need one before) and the four `cta_*` config values, and pad their music-bed-prep duration by `cta_duration` so the bed doesn't run out before the CTA ends. **Known gap, not addressed here:** the CTA isn't reflected in `core/youtube_meta.py`'s `segment_order` (the Summary "content order" exposed over the YouTube-metadata API) — that was already documented as an approximation, not exact rendered timestamps (see the M5-remainder roadmap row), so this just makes that gap very slightly larger, not a new kind of gap. **Verified locally** (`tests/backend/test_summary_cta.py`: `build_summary`'s filter_complex includes the CTA image input and its `concat=n=` count includes it, the music mix runs *after* (not before) that concat so it still covers the CTA, `build_fullflight_with_music` makes exactly 3 ffmpeg calls — stream-copy mix, separately-encoded CTA, stream-copy concat-join — with the main video never re-encoded) — not yet deployed. |
| M26 | **Music credits: strip the catalog number, add the artist.** A real deployed credits box showed raw, numbered, artist-less lines (`- "27 B-Roll"`) — the M18 filename parse only handled the minority of StreamBeats files that spell the artist out (`"21. Harris Heller - High Tide.mp3"`); most are just `"<N> <Title>.<ext>"` with no artist in the name at all. Since the whole catalog is authored by one person (Harris Heller, owner of Senpai Music Group LLC — the license's signatory), `music.resolve_track_credits()`/`normalize_music_credits()` now always strip a leading catalog-number prefix off the title (tag- or filename-derived) and fall back to a new `DEFAULT_ARTIST = "Harris Heller"` whenever no real artist is resolvable, rather than leaving the byline off — matching clause 7's ask to credit "the name of the author". `normalize_music_credits()` repairs already-stored (pre-fix) dict rows the same way, so previously-built Shorts show corrected credits without a rebuild; Summary/FullMusic pick it up on their next build (their credits are file-backed, not DB-backed — see the M18 note above). **Verified locally** (`tests/backend/test_music.py`: unlabeled `"27 B-Roll.mp3"`/`"12 Pastel Blue.mp3"` resolve to title `"B-Roll"`/`"Pastel Blue"` + artist `"Harris Heller"`; a stale stored `{"title": "27 B-Roll", "artist": None}` row is repaired on normalize; a track with a real, different artist is left alone) — not yet deployed. |

### Roadmap (ordered, not yet shipped)

| Milestone | Scope | Exit criteria |
|---|---|---|
| M5 remainder | Exact Summary-video chapter timestamps (needs the build-time `target_seconds` persisted so `auto_fill` filler placement is known — currently only content-order is derivable, see M5a) | chapter timestamps match the real rendered Summary.mp4 |
| Host migration (mostly done) | **App is deployed and healthy on the new host** (`sdh`, `vf.lenti.cloud`, 55 TB pooled storage — see `architecture.md` Deployment). As of the 2026-08-21 recheck: running `v0.4.8`, encoder reports `h264_qsv`, and real builds (Summary with music, Full-flight+music, 12/12 highlight shorts) have completed successfully on real flight footage — login/upload/build all verified live, repeatedly. Still open: a real Flightlog API call has not been exercised; the external YouTube-management repo has not yet actually called `/api/integration/v1` (M12); the Traefik 60s upload timeout fix (a `serversTransport` with `responseHeaderTimeout` disabled for the `vidfactory` service, in the separate infra stack `/opt/sdh.lol/compose/infrastructure/traefik/config/dynamic.yml`, not this repo) is still on the user to apply — M11's chunked upload works around it client-side in the meantime | a real Flightlog-linked project verified end-to-end on `sdh`; the YouTube-management repo successfully pulling metadata via `/api/integration/v1` |

---

## Host setup (Fedora `xpsex`) — STALE, host retired

> `xpsex` is no longer available (home network/host changed after a move). Kept below only as a
> record of the quirks that applied to it; do not deploy against it. The next deploy target is a
> new host (55 TB storage, not yet detailed here) — see the "Host migration" roadmap item above.
- **Mount the `pg` SMB share** at `/mnt/pg` (NAS exports only `/volume1/backup` over NFS; `pg` is
  SMB-only, so it needs CIFS credentials). Until mounted, `/data/{InstaOut,Music,Summaries,Shorts}`
  are missing and `/health` reports `degraded` (still HTTP 200; the app stays reachable). `Archive`
  works because the app creates it on local disk.
- **NVIDIA NVENC — host-side fragile in two distinct ways:**
  1. After a **driver update** the CDI spec goes stale; regenerate it:
     `sudo nvidia-ctk cdi generate --output=/etc/cdi/nvidia.yaml`.
  2. After a **plain reboot** the `nvidia_uvm` module loads lazily, so `/dev/nvidia-uvm` is missing and
     the container fails to start with `CDI device injection failed … "/dev/nvidia-uvm": no such file or
     directory` — even though `nvidia-smi` works (base modules loaded, UVM not). Fix:
     `sudo modprobe nvidia_uvm && sudo nvidia-modprobe -u -c=0` (then regenerate the CDI spec).
  Dev compose uses the CDI device `nvidia.com/gpu=all`; the app selects `h264_nvenc`.

## Backlog (unordered)

- **Speech-AI auto-highlights** (Whisper + Silero VAD) — port `PS_VidAggregator/SpeechSegmentExtractor.ps1`;
  optional deps. Use case: a 3 h flight where only the spoken segments should be exposed.
- **YTChannelMgmt-side reader** for the `metadata.json` handoff (lands in that repo).
- **Multi-device / job-resume polish** — confirm a running job is visible from a second device after refresh.
- **Linux/Fedora hardening** — reverse-proxy auth (Pocket-ID), backups of the SQLite DB on `Archive/`.

(Flugbuch/flight-log-specific backlog items — remaining Excel sheets, multi-flight-day IGC
matching, etc. — now belong to the separate Flightlog project, not here.)
